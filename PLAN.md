# 日本語学習サイト — フロントエンド開発 Plan（studyJP-Font）

> 本ドキュメントはフロントエンドの全体開発計画であり、5つのフェーズに分けて進める。各フェーズが完了しテストに合格してから次のフェーズへ進むこと。
> バックエンドのAPI仕様は `../studyJP-back/PLAN.md` を参照。

>  http://localhost:3000/admin/login からログインできる。admin / Admin@123456
>  ユーザー側のログイン/登録は独立したページ（`src/app/(auth)/`、全画面中央表示、サイト共通フレーム無し）で、URL は /login、/register のまま。ログイン後は router で /articles に遷移する。

## 技術選定

| 項目 | 選択 | 理由 |
|------|------|------|
| フレームワーク | React 18 + Next.js 14（App Router） | 要件で指定；ファイルベースルーティングのため react-router 不要 |
| UIライブラリ | MUI 5（@mui/material + @mui/icons-material） | 要件で指定；DataGrid は管理画面のテーブルに利用可能 |
| HTTP | axios | インターセプターで token とエラーを一元処理 |
| 状態管理 | Zustand | 軽量で、ログイン状態など少数のグローバル状態管理のみに必要十分 |
| リッチテキストエディタ | wangEditor 5 | MUI にはリッチテキストコンポーネントが無い；wangEditor は軽量でドキュメントも充実 |
| 言語 | TypeScript | インターフェースの型をバックエンドの DTO と揃え、初歩的なミスを削減 |

補足：

- ユーザー側と管理側は**同一の Next.js プロジェクト**内に配置し、ルートプレフィックスで区別する（`/` がユーザー側、`/admin` が管理側）。token とログイン体系は完全に分離する（それぞれ `user_token` / `admin_token` として保存）。
- 本プロジェクトはログイン後に使用する学習/管理システムであり、SEO に依存しないため、全ページで**クライアント側レンダリング**を統一採用する（`"use client"` ＋ クライアント側からバックエンド API を呼び出す）。SSR でのデータ取得は行わず、サーバー側/クライアント側の両方で token を管理する複雑さを避ける。
- 開発環境では `next.config.js` の `rewrites` により `/api/**` を `http://localhost:8080` へ転送し、クロスオリジン問題を回避する。

## ディレクトリ構成

```
studyJP-Font/
├── next.config.js           # rewrites: /api → http://localhost:8080
├── package.json
├── tsconfig.json
└── src/
    ├── app/                          # App Router ページ（ファイルベースルーティング）
    │   ├── layout.tsx                # ルートレイアウト（MUI ThemeProvider、CssBaseline）
    │   ├── page.tsx                  # トップページ → /articles へリダイレクト
    │   ├── (user)/                   # ユーザー側グループ
    │   │   ├── layout.tsx            # ユーザー側レイアウト（上部 AppBar ナビゲーション）＋ ユーザーガード
    │   │   ├── login/page.tsx
    │   │   ├── register/page.tsx
    │   │   └── articles/
    │   │       ├── page.tsx          # コース一覧
    │   │       └── [id]/page.tsx     # コース詳細（音声再生を含む）
    │   └── admin/                    # 管理側
    │       ├── layout.tsx            # 管理側レイアウト（サイドDrawerメニュー）＋ 管理ガード
    │       ├── login/page.tsx
    │       ├── users/page.tsx        # ユーザー管理
    │       └── articles/
    │           ├── page.tsx          # 記事一覧（状態絞り込み）
    │           └── edit/[[...id]]/page.tsx  # 新規作成/編集（リッチテキスト＋音声アップロード）
    ├── api/                 # API 呼び出しのラッパー
    │   ├── request.ts       # axios インスタンス（ユーザー側、user_token を保持）
    │   ├── adminRequest.ts  # axios インスタンス（管理側、admin_token を保持）
    │   ├── user.ts          # ログイン/登録
    │   ├── article.ts       # 記事一覧/詳細
    │   └── admin/           # 管理側API（ユーザー管理、記事管理、アップロード）
    ├── store/
    │   ├── userAuth.ts      # ユーザー側ログイン状態
    │   └── adminAuth.ts     # 管理側ログイン状態
    ├── components/
    │   ├── AuthGuard.tsx    # クライアント側ルートガード（ユーザー側/管理側で共用、role を渡す）
    │   └── AudioPlayer.tsx  # 音声プレイヤー
    ├── theme/
    │   └── index.ts         # MUI テーマのカスタマイズ
    └── types/               # バックエンドの DTO に対応する型定義
```

## 共通の約束事

- 全ての一覧ページはページングに対応し、パラメータは統一して `page`（1始まり）＋ `pageSize` とする。
- バックエンドの共通レスポンス形式は `{ code, message, data }` で、axios インターセプターで統一的に解包する。`code === 401` の場合は token をクリアし、対応するログインページへ遷移する。
- ログインガードは各グループの `layout.tsx` 内でクライアントコンポーネントとして実装する：token が無い場合は `router.replace` で対応するログインページへ遷移する。

---

## フェーズ1：基盤構築

**目標**：プロジェクトが起動し、ルーティングの骨格が動作すること。

1. `npx create-next-app@latest`（TypeScript + App Router + src ディレクトリ）を実行し、@mui/material、@emotion/react、@emotion/styled、@mui/icons-material、axios、zustand をインストールする。
2. MUI を設定：ルートレイアウトに `ThemeProvider` ＋ `CssBaseline` を配置し、Next.js の `@mui/material-nextjs` App Router アダプタ（emotion スタイルのキャッシュ）を導入する。
3. `next.config.js` の rewrites を設定：`/api/:path*` → `http://localhost:8080/api/:path*`。
4. ルーティングの骨格を構築：ユーザー側レイアウト（AppBar）、管理側レイアウト（Drawer）＋ 空のプレースホルダーページ。
5. axios インスタンスと共通レスポンス処理をラップする。

**受け入れ基準**：`npm run dev` で起動し、ユーザー側と `/admin` 間を遷移でき、MUI のスタイルがちらつきなく正常に表示されること。

## フェーズ2：ユーザー側ログイン＋権限体系

**目標**：登録、ログイン、ルートガードが利用可能になること。

1. 登録ページ：ユーザー名/メールアドレス/パスワード＋パスワード確認。MUI TextField ＋ react-hook-form でバリデーションし、`POST /api/user/register` を呼び出す。
2. ログインページ：`POST /api/user/login` を呼び出し、成功時は token を localStorage（キー：`user_token`）に保存し、ユーザー情報を Zustand に保存する。
3. axios のリクエストインターセプターで自動的に `Authorization: Bearer <token>` を付与する。
4. `AuthGuard`：未ログイン状態で保護対象ページにアクセスした場合は `/login` にリダイレクトし、遷移元パスを記録してログイン後に元のページへ戻す。
5. AppBar にログイン状態を表示し、ログアウトできるようにする。

**受け入れ基準**：登録 → ログイン → ページリロード後もログイン状態が保持される → ログアウト後は保護対象ページへのアクセスが遮断されること。

## フェーズ3：記事一覧＋文単位で読み進める読書ページ（音声再生を含む）

**目標**：学習者がコースを閲覧し、記事を1文ずつ読みながら音声を聴けること。

1. **一覧ページ** `/articles`
   - `GET /api/user/articles?page=&pageSize=&level=&category=`
   - MUI Card ＋ Grid によるカード表示（カバー画像、タイトル、レベル Chip、カテゴリ）。
   - 上部の絞り込み：レベル（N5–N1）、カテゴリ（Select）；MUI Pagination でページング、絞り込み条件は URL クエリに同期する。
2. **読書ページ** `/articles/[id]`（バックエンドの実APIに接続済み、mock は削除済み）
   - `GET /api/user/articles/{id}` はリッチテキストの `content` ＋ 複数の `audios` を返す。
   - **文単位表示（自動分文）**：フロント側でリッチテキストを `。！？` により自動的に分文し（`lib/articleContent.ts` の `parseReaderSentences`）、各文を1つの `SentenceItem`（日本語文＋中国語訳の3状態）として表示する。**管理画面では段落単位でそのまま貼り付ければよく、手動での分割は不要**。
   - **日中対訳**：管理画面で独立した「中国語訳」リッチテキスト（`article.translation`）を入力し、中国語も同様に `。！？` で自動分文した後、日本語の文と順番通りに一対一で対応付ける（段落数が一致する場合は段落単位、その後文単位で対応付け；一致しない場合は全文を文単位で順番に対応付け、余った中国語文は最後の文に結合する）。旧データについては本文中に埋め込まれた中国語段落にも対応する。通訳（翻訳）の3状態：表示（中国語訳をクリックするとその文だけ非表示にできる）／クリックして表示（デフォルトで非表示にし、「（クリックして翻訳を表示する）」というプレースホルダーを表示、クリックで展開）／非表示。
   - **入力時の約束事**：日本語と中国語訳はそれぞれ段落単位でそのまま貼り付ければよく、両者の文の数が `。！？` で分割した際に一致していれば文単位で対応付けられる。
   - **振り仮名注記＋品詞着色**：フロント側の kuromoji による分かち書きで生成する（`lib/furigana.ts`、辞書は `public/dict` 内で約17MB、`postinstall` で node_modules からコピーし gitignore 済み）。「あ カタカナ」ボタンで振り仮名の表示/非表示をグローバルに切り替える。品詞の背景色は**常時表示**：名詞 `#ffe3bd` オレンジ／動詞 `#cdeccd` 緑／形容詞 `#f9d8e5` ピンク／外来語（カタカナのみ）`#c3e7fb` 青。
   - **再生に合わせたハイライト（概算）**：文単位のタイムラインが無いため、文字数の比率で音声の長さを各文に按分する（`indexAtFraction`/`startFractionOf`）。現在読み上げ中の文は文全体の背景を `#dceafe` でハイライトし自動的に画面中央へスクロールする。文をクリックすると概算した開始位置からジャンプ再生する。バックエンドの対応データが揃い次第、正確な同期に置き換える。
   - `AudioPlayer` の下部固定プレイヤー：進捗バーのドラッグによる ±10秒シーク、再生/一時停止、±30秒シーク、7段階の速度切り替えと翻訳モードの bottom-sheet、集中リスニングへの入口；複数音声は Chip でトラックを切り替える。
   - 集中リスニングモード：全画面の単文カード（進捗バッジ n/N、文は `。！？` で分割）、2秒巻き戻し、前/次の文（概算ジャンプ再生）、再生中の自動ページ送り（概算）、テキスト非表示での聴き取り練習。

**受け入れ基準**：絞り込み＋ページングが正しく動作しリロード後も保持されること；管理画面で公開した記事がユーザー側で閲覧可能で下書きは非表示であること；振り仮名注記/品詞着色/段落翻訳/速度変更/集中リスニングが利用可能であること；再生時に段落の背景ハイライトが追従すること；音声の再生/一時停止、進捗のドラッグ、複数トラックの切り替えができること。

## フェーズ4：管理システム

**目標**：管理者がログイン後にユーザーと記事を管理できること。

1. **管理側ログイン** `/admin/login`：`POST /api/admin/login` を呼び出し、token を `admin_token` に保存し、ユーザー側とは完全に分離する；管理側の `layout.tsx` のガードで全ての `/admin/*` ページを保護する。
2. **ユーザー管理** `/admin/users`
   - MUI Table/DataGrid によるページング一覧（ユーザー名、メールアドレス、状態、登録日時）。
   - 新規作成/編集（Dialog フォーム）、削除（ソフトデリート、確認 Dialog）、有効/無効の切り替え（Switch）。
3. **記事管理** `/admin/articles`
   - 一覧：状態絞り込み（下書き/公開済み）、レベル/カテゴリ絞り込み、ページング。
   - 新規作成/編集ページ `/admin/articles/edit/[id]`：
     - タイトル、レベル、カテゴリ、カバー画像アップロード、状態（下書き保存/公開）。
     - wangEditor によるリッチテキスト本文（`dynamic import` で SSR 読み込みを無効化）。
     - 音声アップロード：`POST /api/admin/upload/audio` を呼び出し、返された URL を記事に紐付ける。複数音声のアップロード、並べ替え・削除に対応。
   - 削除：ソフトデリート、二段階確認。

**受け入れ基準**：管理画面での CRUD 一連の流れが利用可能であること；ユーザー側の token では管理側のページとAPIにアクセスできないこと；下書きがユーザー側の一覧に表示されないこと。

## フェーズ5：問題演習（刷題）モジュール

**目標**：学習者が問題演習（単選/多選）を行い、回答直後に正誤マークと解析を確認し、答題カードと練習結果で振り返れること。管理者は問題バンクを管理できること。
UI 詳細は `.github/skills/quiz-practice-page/SKILL.md`（刷題ページ）と `.github/skills/quiz-answer-sheet/SKILL.md`（答題カード/結果）を正とし、実装は `quiz-frontend-dev` agent の規約に従う。API 契約はバックエンド `.github/skills/quiz-module/SKILL.md` を参照。

1. **練習開始入口** `/practice`
   - 「練習を開始」ボタン → `POST /api/user/practices` → `/practice/[id]` へ遷移。過去の練習一覧（続きから/結果を見る）は任意拡張。
2. **刷題ページ** `/practice/[id]`（1画面1問）
   - ヘッダー：進捗 `n / N`；題幹に `単選`/`多選` バッジ + `題目 #id`。
   - 提出フロー：単選はタップで即提出；多選はトグル選択 → 「確認」ボタンで提出。提出後は選択変更不可。
   - 回答後マーク（4状態）：選択&正解=緑丸✓／選択&不正解=赤✗／未選択の正解=緑塗りラベル（見逃し）／その他=通常。フッターに `正確答案 X 您選択 Y`（正解は常に緑、您選択は全体正誤で緑/赤）。
   - **試題詳解**：回答後のみ「原解析」を表示。未回答時は正解・解析データがフロントに存在しないこと。
   - 前後の問題ナビ + 答題カードを開くボタン。リロード後も回答済み状態を API から復元。
3. **答題カード + 練習結果ページ** `/practice/[id]/result`
   - 答題カード：凡例（未作答=白/答対=緑/答錯=赤）、1行5個の円形番号グリッド、現在問題に「当前」バッジ、番号タップでジャンプ。
   - 下部ボタン：重新練習（確認 → `retry` → 新練習へ）／査看練習結果。
   - 結果ページ：正答率 + 問題数/答対/答錯/未回答の集計、3色グリッド再掲、錯題一覧（タップで見直し）。
4. **管理側：問題管理** `/admin/questions`
   - 一覧（type/category/status 絞り込み + ページング）、新規/編集（題幹、種別、選択肢2〜6件の追加/削除/正解チェック、解析、下書き/公開）、論理削除（二段階確認）。
   - 単選=正解1件、多選=正解2件以上のフロント側バリデーション（react-hook-form）。

**受け入れ基準**：単選/多選の提出フローとマークがスクリーンショット通りであること；多選の部分一致で「見逃した正解」が緑塗りラベルで表示されること；提出前に正解・解析が画面に現れないこと；答題カードの3色と集計が実績と一致すること；重新練習で全問未回答の新練習が始まること；管理画面で作成した公開問題だけが練習に出題されること。

## フェーズ6：分類管理（辞書）モジュール

**目標**：管理者が記事レベル(N5等)・記事分類・問題分類を増削改でき、既存の記事一覧/記事管理/記事編集/問題管理/問題編集がハードコード配列ではなく本モジュールのデータを参照するようになること。
UI 詳細・既存ページの置き換え対応表は `.github/skills/category-management/SKILL.md` を正とし、実装は `category-admin-dev` agent の規約に従う。API 契約はバックエンド `.github/skills/category-management/SKILL.md` を参照。

1. **管理画面** `/admin/categories`
   - scope タブ（記事レベル/記事分類/問題分類）+ 一覧テーブル（value、有効/無効、更新日時）。
   - 新規追加・編集（value + 有効/無効）・論理削除（確認ダイアログ、使用中はバックエンドの400メッセージをそのまま表示）。
2. **既存5ページの置き換え**（ハードコードされた `LEVELS`/`CATEGORIES` 定数を削除しAPI取得に統一）
   - `(user)/articles`、`admin/articles`、`admin/articles/edit/[[...id]]`、`admin/questions`、`admin/questions/edit/[[...id]]`。
   - 問題編集の「分類」は自由入力 TextField から選択式（空欄可）に変更する。
3. **API/型**：`src/api/category.ts`（ユーザー側）、`src/api/admin/category.ts`（管理側）、`src/types/category.ts`。
4. `admin/layout.tsx` の Drawer に「分類管理」ナビを追加する。

**受け入れ基準**：3 scope それぞれで増削改が画面から行えること；追加したカテゴリが対応する既存ページのセレクト/フィルタに反映されること；使用中カテゴリの削除が拒否されエラーメッセージが表示されること；無効化したカテゴリは新規作成の選択肢から消えるが既存データの表示は壊れないこと。

## フェーズ7：英語精読モジュール

**目標**：学習者が英語記事を文単位で読み進めながら音声を聴け、当前朗読中の文が赤くハイライトされ、句中の難語をクリックすると単語表ボトムシートが開いて音標・品詞・中国語釈義・英中対訳例文を確認できること。日本語読書ページとは独立したモジュールとして実装し、既存ページに回帰を起こさないこと。
UI詳細は `.github/skills/english-reading-page/SKILL.md` を正とし、実装は `english-reading-frontend-dev` agent の規約に従う。API契約はバックエンド `.github/skills/english-reading-module/SKILL.md` を参照。

1. **一覧ページ** `/en-articles`
   - `GET /api/user/en-articles?page=&pageSize=&level=&category=`
   - 日本語版の記事一覧と同様の Card + Grid 表示（レベルは CEFR A1〜C2 の Chip）。
2. **読書ページ** `/en-articles/[id]`
   - `GET /api/user/en-articles/{id}` は `content`（英語）+ `translation`（中国語）+ `audios[]` + `words[]`（`sentenceIndex` で文に対応）を返す。
   - 文単位表示（自動分文、`parseEnglishSentences`）＋当前朗読中の文の赤ハイライト（概算按分は日本語版と同じ方式、配色トークンは別管理）。
   - 句中難語の青ハイライト（クリック可能）＋単語表ボトムシート（底部「単語」ボタンと句中クリックの2入口、同一コンポーネント）。単語カードは「単語+音標+品詞+中国語釈義+英中対訳例文」。
   - 下部プレイヤーはツールバー4項目のみ（速度/集中聴力/翻訳/単語）、速度・集中聴力・翻訳は日本語版のコンポーネントを再利用。
   - **跟読ハイライト**（追記）：日本語版読書ページの単語追従ハイライト（青枠線）を移植。kuromojiトークンが無いため文字クラス正規表現で単語分割する専用の `tokenizeEnglishSentence`/`activeWordIndexInSentence` を実装。
3. **管理側：英語記事管理** `/admin/en-articles`（任意拡張、フェーズ4の記事管理と同様の一覧+新規/編集+音声アップロード+単語表の追加/削除フォーム）

**受け入れ基準**：一覧の絞り込み+ページングが正しく動作すること；読書ページで句中の難語クリック→単語表ボトムシートが該当単語にスクロール+ハイライトすること；底部「単語」ボタンから開くとリスト先頭から表示されアクティブな単語が無いこと；再生位置に応じて赤ハイライトが追従すること；日本語版読書ページの表示・挙動に回帰が無いこと。

## フェーズ8：ホームページモジュール

**目標**：トップページ（`/`）が単なるリダイレクトではなく、管理画面で登録した主図バナーの自動再生スライダー、日本語コース／英語精読／問題演習の3ブロックのプレビューを持つ実際の入口ページになること。表示文言はすべて日本語（中国語不可）。
UI詳細は `.github/skills/homepage-page/SKILL.md` を正とし、実装は `homepage-frontend-dev` agent の規約に従う。API契約はバックエンド `.github/skills/homepage-module/SKILL.md` を参照。

1. **トップページ** `/`（`redirect("/articles")` を削除して置き換える）
   - 主図スライダー：`GET /api/user/home-banners` を取得し自動再生（4秒）＋ドラッグ/スワイプ＋ドット操作。`linkType` に応じて `/articles/{id}`・`/en-articles/{id}`・`/practice`・外部URLへ遷移。
   - クイックリンク行：日本語コース／英語精読／問題演習の円形アイコン3つ。
   - 3ブロックのプレビューカード（**縦積み、PC幅でも横並びグリッドにしない**）：日本語コース・英語精読は既存の記事一覧APIから最新4件（モバイルは2件）を取得してカバー画像+タイトルを表示、問題演習は固定タイル。各ブロックに「もっと見る」で対応する一覧ページへ。
2. **トップナビ**：`(user)/layout.tsx` の AppBar に「ホーム／コース／問題演習／英語精読」を追加するが、PC幅（`md` 以上）でのみ表示し、モバイル幅では非表示にする。
3. **管理側：主図バナー管理** `/admin/home-banners`（一覧+新規/編集フォーム：タイトル・補足・タグ・画像アップロード・リンク種別/リンク先・並び順・有効無効、論理削除）

**受け入れ基準**：バナー0件でもエラーにならずスライダーが非表示になること；各 `linkType` のバナークリックが正しい遷移先に飛ぶこと；主図のドラッグ/スワイプでスライド切り替えができること；PC幅でトップナビが表示されモバイル幅で非表示になること；日本語コース/英語精読ブロックに実際の最新記事が表示されること；管理画面でバナーを無効化するとユーザー側で即座に消えること；ページ全体に中国語が含まれないこと。

---

## フェーズ9：権限モジュール（コンテンツの無料/VIP + 管理画面のロール別メニュー）

**目標**：①学習者向けの記事一覧・英語精読一覧にVIP限定バッジを表示し、VIP限定コンテンツの詳細ページは無料会員に専用の案内UIを出すこと。②ユーザー管理画面でVIP資格（期限付き）を付与できること。③管理画面のDrawerメニューが管理者のロール（SUPER_ADMIN/CONTENT_ADMIN/USER_ADMIN）に応じて絞り込まれ、権限外URLへの直接アクセスがリダイレクトされること。④管理者アカウント自体を管理する新規ページ（SUPER_ADMIN専用）。既存の記事一覧/詳細・英語精読一覧/詳細・問題演習・ユーザー管理・admin レイアウトを横断して変更する。
詳細仕様は `.github/skills/permissions-module/SKILL.md` を正とし、実装は `permissions-frontend-dev` agent の規約に従う。API契約はバックエンド `.github/skills/permissions-module/SKILL.md` を参照。

1. **型拡張**：`UserInfo` に `vip`/`vipExpireAt`、`AdminInfo` に `role`、記事/英語精読/問題関連の型に `accessLevel` を追加。
2. **学習者向けVIPロックUI**：一覧のVIPバッジ、詳細ページの403専用案内UI（既存の汎用エラーAlertと区別する）。
3. **ユーザー管理へのVIP設定UI** `/admin/users`：VIP列 + 期限設定Dialog（`PUT /api/admin/users/{id}/vip`）。
4. **管理画面メニューのロール制御**：`admin/layout.tsx` の `MENU` に `roles` を追加してDrawerを絞り込み、権限外パスへの直接アクセスは `/admin` へリダイレクト（UX目的、真の防御はバックエンド）。
5. **管理者アカウント管理**（新規）`/admin/admins`（SUPER_ADMIN専用、`/admin/categories` と同様のDialogベースCRUD）。
6. **コンテンツ編集フォームに公開レベル追加**：`/admin/articles/edit`・`/admin/en-articles/edit`・`/admin/questions/edit` に無料/VIP選択を追加。

**受け入れ基準**：無料会員でVIP限定記事を開くと専用案内UIが出ること；VIP会員は問題なく閲覧できること；`CONTENT_ADMIN`/`USER_ADMIN` でログインするとDrawerメニューが絞り込まれ権限外URLへの直接アクセスがリダイレクトされること；ユーザー管理でVIP設定した内容が一覧に反映されること；記事編集で公開レベルを変更すると一覧のバッジに反映されること。

## フェーズ10：問題演習の学科（大分類）モジュール

**目標**：`/practice` エントリページを学科（日本語/英語/AWS等）→分類の2階層カード選択に変更し、管理画面（分類管理・問題管理）で学科を扱えるようにする。新しいページは作らず、既存の `/practice`・`admin/categories`・`admin/questions` を拡張する。
詳細仕様は `.github/skills/question-subject-page/SKILL.md` を正とし、実装は `question-subject-frontend-dev` agent の規約に従う。API契約はバックエンド `.github/skills/question-subject-module/SKILL.md` を参照。

1. **型・APIクライアント拡張**：`CategoryScope` に `QUESTION_SUBJECT` 追加、`fetchCategories`/`fetchAdminCategories` に `subject` 引数追加、問題関連の型に `subject` 追加。
2. **`/practice` エントリページ**：学科カード選択→分類カード選択の2階層に変更（コンポーネント内 state、URLクエリは使わない）。
3. **管理画面：分類管理**：scope タブに「問題学科」追加。「問題分類」タブでは学科サブセレクタで分類辞書を絞り込む。
4. **管理画面：問題編集・一覧**：学科 select（必須、分類の選択肢は学科に連動）、一覧に学科フィルタ・学科列を追加。

**受け入れ基準**：`/practice` で学科→分類→開始の一連が動作し、出題される問題が選択した学科・分類と一致すること；管理画面で学科ごとに分類辞書が独立して見えること；問題編集で学科を変えると分類の選択肢が切り替わり旧学科の分類値が残らないこと。

## フェーズ11：日本語記事の単語表モジュール

**目標**：既存の日本語読書ページ（`/articles/[id]`）に単語表（生词表）機能を追加する。句中で単語表に登録された単語には左上に常時マーカーが付き、クリックすると単語表ボトムシートが開いて漢字表記・ふりがな・品詞・中国語釈義・日中対訳例文を確認できる。英語精読モジュールのような独立モジュールの新設ではなく、既存の `article`/`SentenceItem`/`AudioPlayer` を直接拡張する。
詳細仕様は `.github/skills/article-word-page/SKILL.md` を正とし、実装は `article-word-frontend-dev` agent の規約に従う。API契約はバックエンド `.github/skills/article-word-module/SKILL.md` を参照。

1. **型・APIクライアント拡張**：`src/types/index.ts` に `WordItem` を追加し、`ArticleDetail`/`AdminArticleDetail`/`ArticleSavePayload` に `words` を追加（`enArticle.ts` のような独立ファイルは作らない）。
2. **句中単語マーカー**：`lib/articleContent.ts` に `matchVocabInSentence(rubyWords, words)` を追加。kuromoji トークン列の文字オフセットで見出し語を照合し（`sentenceIndex` では絞り込まない）、連続トークンをグループ化する。`components/SentenceItem.tsx` に `renderVocabRubyWords` を追加し、マッチした単語グループの左上に丸マーカーを常時表示、クリックで単語表を開く（`stopPropagation` で文のジャンプ再生を誘発しない）。
3. **単語表ボトムシート** `components/WordSheet.tsx`（新規）：下部プレイヤーの「単語」ボタン（入口A）と句中クリック（入口B）の2入口から同一コンポーネントを開く。カードは単語（漢字表記）＋ふりがな／品詞＋中国語釈義／例文（日本語＋中国語訳）。
4. **下部プレイヤー拡張**：`components/AudioPlayer.tsx` のツールバーに5項目目「単語」を追加。
5. **管理側：記事編集フォームの拡張** `/admin/articles/edit`：`/admin/en-articles/edit` の単語リスト編集UI（追加/削除、行ごとのフォーム）を踏襲し、フィールドラベルを日本語版（単語/ふりがな/品詞/中国語释义/例文（日本語）/例文（中国語））に差し替える。

**受け入れ基準**：句中の単語マーカーをクリック→単語表ボトムシートが該当単語にスクロール＋ハイライトすること；底部「単語」ボタンから開くとリスト先頭から表示されアクティブな単語が無いこと；単語マーカーが朗読中かどうかに関わらず常時表示されること；単語クリックが文のジャンプ再生を誘発しないこと；送り仮名・活用形が本文と完全一致しない単語は句中でハイライトされないこと；既存の英語精読モジュールの表示・挙動に回帰が無いこと。

---

## フェーズ間の依存関係について

- フェーズ1はバックエンドに依存しない；フェーズ2以降は対応するバックエンドのAPIが整っている必要がある。
- バックエンドのAPIが未整備の場合、`api/` 層で一時的にダミーデータを返すようにし、結合テスト時に取り除く。
